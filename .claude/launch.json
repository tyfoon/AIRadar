{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "airadar",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "set -a && source .env && set +a && python3 -m uvicorn api:app --host 0.0.0.0 --port 8000"],
      "port": 8000
    }
  ]
}
